// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Belt_Option from "rescript/lib/es6/Belt_Option.js";

function assembleParts() {
  return "ASSEMBLED";
}

function fuel(ironSuit) {
  return "FUELED";
}

function suitUp(ironSuit, stark) {
  return "IRONMAN_SUITEDUP";
}

let SuitUp = {
  assembleParts: assembleParts,
  fuel: fuel,
  suitUp: suitUp
};

function preLaunchChecks(ironMan) {
  return "IRONMAN_PRELAUNCH_CHECKED";
}

function startEngine(ironMan) {
  return "IRONMAN_ENGINE_STARTED";
}

let EngineStart = {
  preLaunchChecks: preLaunchChecks,
  startEngine: startEngine
};

function rangeVerification() {
  return {
    x: 12,
    y: 14
  };
}

function launch(ironMan, coordinates) {
  return "LAUNCHED";
}

let Launch = {
  rangeVerification: rangeVerification,
  launch: launch
};

function tonySuitUp(__x) {
  return suitUp(__x, "TonyStark");
}

let startedEngine = Belt_Option.flatMap(Belt_Option.flatMap(Belt_Option.flatMap(Belt_Option.flatMap("ASSEMBLED", fuel), tonySuitUp), preLaunchChecks), startEngine);

function launch$1(coords) {
  return Belt_Option.flatMap(startedEngine, i => launch(i, coords));
}

let program = Belt_Option.flatMap({
  x: 12,
  y: 14
}, launch$1);

console.log(program);

export {
  SuitUp,
  EngineStart,
  Launch,
  program,
}
/* startedEngine Not a pure module */
