// Generated by ReScript, PLEASE EDIT WITH CARE


function identity(a) {
  return a;
}

function ask() {
  return identity;
}

function from(a) {
  return param => a;
}

function map(fa, f) {
  return r => {
    let match = fa(r);
    return [
      f(match[0]),
      match[1]
    ];
  };
}

function chain(fa, f) {
  return r => {
    let match = fa(r);
    return f(match[0])(match[1]);
  };
}

let State = {
  ask: ask,
  asks: identity,
  from: from,
  map: map,
  chain: chain
};

let asks = identity;

export {
  identity,
  State,
  ask,
  asks,
  from,
  map,
  chain,
}
/* No side effect */
