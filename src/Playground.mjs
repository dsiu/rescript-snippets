// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Belt_List from "rescript/lib/es6/Belt_List.js";
import * as Belt_Array from "rescript/lib/es6/Belt_Array.js";

function dup(x) {
  return [
    x,
    x
  ];
}

let src = [
  2,
  4,
  6
];

function flatMap(m, f) {
  return Belt_Array.reduce(m, [], (a, x) => Belt_Array.concat(a, f(x)));
}

console.log(Belt_Array.map(src, dup));

console.log(flatMap(src, dup));

function dupl(x) {
  return {
    hd: x,
    tl: {
      hd: x,
      tl: /* [] */0
    }
  };
}

let srcl = {
  hd: 2,
  tl: {
    hd: 4,
    tl: {
      hd: 6,
      tl: /* [] */0
    }
  }
};

function flatMapL(m, f) {
  return Belt_List.reduce(m, /* [] */0, (a, x) => Belt_List.concat(a, f(x)));
}

console.log(Belt_List.toArray(Belt_List.map(srcl, dupl)));

console.log(Belt_List.toArray(flatMapL(srcl, dupl)));

export {
  dup,
  src,
  flatMap,
  dupl,
  srcl,
  flatMapL,
}
/*  Not a pure module */
