// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Caml_obj from "rescript/lib/es6/caml_obj.js";
import * as Caml_option from "rescript/lib/es6/caml_option.js";
import * as PervasivesU from "rescript/lib/es6/pervasivesU.js";

function empty(k) {
  
}

function add(d, k, v) {
  return function (l) {
    if (Caml_obj.equal(l, k)) {
      return Caml_option.some(v);
    } else {
      return d(l);
    }
  };
}

function find(d, k) {
  return d(k);
}

var despicable = add(add(add(empty, "Felonius Gru", 53), "Dave the Minion", -2147483648), "Dr. Nefario", 80);

console.log((function (x) {
          if (x !== undefined) {
            return x;
          } else {
            return PervasivesU.failwith("Not found");
          }
        })(despicable("Dave the Minion")));

export {
  empty ,
  add ,
  find ,
  despicable ,
}
/* despicable Not a pure module */
